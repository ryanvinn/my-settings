" To disable compatibility with vi
set nocompatible

" To enable type file detection
filetype on

" To enable plugins and load plugin for the detected file type
filetype plugin on

" To load and indent file for the detected file type
filetype indent on

" To turn syntas highlighting on
syntax on

" To add numbers to each line on the left-hand side
set number

" To add relative numbers
set relativenumber

" To highlight cursor line underneath the cursor horizontally
set cursorline

" To highlight cursor line underneath the cursor vertically
set cursorcolumn

" To set gruvbox as colorscheme
colorscheme gruvbox

" To set background as dark
set background=dark

" To set colorcolumn to 80
set colorcolumn=80
